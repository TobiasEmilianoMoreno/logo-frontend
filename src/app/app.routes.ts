import { Routes } from '@angular/router';
import { SearchBarComponent } from './features/hero-section/search-bar/search-bar.component';

export const routes: Routes = [
    /* {
        path: 'search',
        component: SearchBarComponent
    },
    {
        path: '**',
        redirectTo: 'search'
    } */
];
